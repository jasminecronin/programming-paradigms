CPSC 449 Assignment 2 - Peg Solitaire Solver

It is not currently possible to input custom games into the program.

To run the 6 game types, call the following functions in the interpreter:

peg(crossbow).
peg(longbow).
peg(almost_dead).
peg(half_dead).
peg(not_quite_dead).
peg(standard).